# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Note: The following are configured in Supabase Edge Functions, not as environment variables:
# - Anthropic API Key (in generate-summary and generate-match-score functions)
# - Apify API Token (in apifyService.ts)
# - Stripe Secret Key (in stripe-checkout and stripe-webhook functions)
# - Stripe Webhook Secret (in stripe-webhook function)